# YouTube Comment sniffer

After all creds have been setup via the Google Developer Console, run this with the following command:

```bash
FLASK_APP=flask_example.py flask run --host=0.0.0.0 --port=8000
```

